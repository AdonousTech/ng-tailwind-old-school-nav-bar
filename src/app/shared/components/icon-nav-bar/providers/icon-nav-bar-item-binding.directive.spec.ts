import { IconNavBarItemBindingDirective } from './icon-nav-bar-item-binding.directive';

describe('IconNavBarItemBindingDirective', () => {
  it('should create an instance', () => {
    const directive = new IconNavBarItemBindingDirective();
    expect(directive).toBeTruthy();
  });
});
